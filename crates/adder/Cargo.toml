[package]
name = "adder"
version = "0.1.0"
edition = "2021"

[lib]
name="adder"
path="src/lib.rs"

[features]
default = ["std"]
std = []
cross-compiled = []

#

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
